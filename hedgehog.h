#ifndef _HEDGEHOG_H_
#define _HEDGEHOG_H_
#include "loadMap.h"

void mobile_hedgehog(char map[][NbCol]);
void moveLeft(char map[][NbCol]);
void moveRight(char map[][NbCol]);
void moveUp(char map[][NbCol]);
void moveDown(char map[][NbCol]);

#endif

